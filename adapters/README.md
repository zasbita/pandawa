# adapters/

Per-host installation notes and edge cases. Executable adapter logic lives in
`src/pandawa_cli/__init__.py` (`AGENT_CONFIG` is the single source of truth).
This directory documents only what does not belong in code.
